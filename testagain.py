def sort(n):
    global lst
    def test(x):
        if x == len(n)-1:
            return lst
        global lst
        lst += n[x].pop()
        if 
        
    test(0)
lst = []
inp = input("Enter Input : ").split(',')
print(sort(inp))